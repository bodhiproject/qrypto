const styles = (theme) => ({
  card: {
    cursor: 'pointer',
    borderRadius: theme.border.radius,
  },
  cardContent: {
    padding: 0,
    background: theme.color.gradientPurple,
  },
});

export default styles;
