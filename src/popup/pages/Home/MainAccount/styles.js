const styles = (theme) => ({
  card: {
    cursor: 'pointer',
    borderRadius: theme.border.radius,
  },
  cardContent: {
    padding: 0,
    background: theme.palette.background.gradient,
  },
});

export default styles;
